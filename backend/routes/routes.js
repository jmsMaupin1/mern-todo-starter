const app = require('express').Router();
const Todos = require('../models/todos');

// List all Todos
app.get('/', (req, res) => {
    
})

// Create a Todo
app.post('/', (req, res) => {
   
})

// Delete a Todo
app.delete('/:id', (req, res) => {
    
})

// Update a Todo
app.put('/:id', (req, res) => {
    
})

module.exports = app;